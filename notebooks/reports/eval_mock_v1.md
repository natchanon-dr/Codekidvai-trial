# Phase 3 — Mock Pipeline Verification Report

> **WARNING: MOCK DATA ONLY**
> This report documents a pipeline smoke test using synthetic data generated by `notebooks/generate_mock_data.py`.
> Metrics shown here are NOT real research findings and must NOT be cited in the thesis.
> Real-data evaluation is pending export from the production system.

---

## 1. Purpose

Verify that the Phase 3 notebook pipeline (M3.1–M3.4) executes end-to-end without errors and that all quality gates, leakage checks, and artifact outputs work correctly before running on real data.

---

## 2. Mock Dataset Summary

| Item | Value |
|---|---|
| Snapshot date | 2026-07-10 |
| Batch code | MOCK001 |
| Session rows | 546 |
| Attempt rows | 2,456 |
| Unique learners | 80 |
| Unique tasks | 8 |
| Batches | MOCK_BATCH_001, MOCK_BATCH_002 |
| Learner groups | G1, G2, G3, G4 |
| Student × task coverage | 546 / 640 (85.3%) |
| **at_risk = 1** | 197 (36.1%) |
| **at_risk = 0** | 349 (63.9%) |
| Imbalance ratio | 1.77 : 1 |
| Missing submission rows | 39 (7.1%) |
| Sessions without attempts | ~22 (4.0%) |
| review_score non-null (of submitted) | 161 / 507 (31.8%) |

---

## 3. Gate Check Results

### NB01 — Load Dataset
| Check | Result |
|---|---|
| Required session columns present | PASS |
| Required attempt columns present | PASS |
| PII-sensitive column check | PASS (warned; no crash) |
| snapshot_metadata.json written | PASS |

### NB02 — Data Quality Check
| Check | Result |
|---|---|
| Null rates computed (session + attempt) | OK |
| at_risk label computed correctly | OK |
| class_balance.png saved | OK |
| learner_group_dist.png saved | OK |
| Quality gate (n_students ≥ 10, n_pairs > 0) | OK — Proceed to 03 |

### NB03 — Feature Engineering
| Check | Result |
|---|---|
| Attempt aggregation (groupby learner × task) | OK |
| Leakage check (no oracle/score columns in X_baseline) | PASS — no leakage |
| GroupShuffleSplit by academy_member_id | PASS — no student overlap |
| Parquet files written (X_baseline_train, X_baseline_test) | OK |
| Label CSV files written (y_train, y_test) | OK |
| split_metadata.json written | OK |

### NB04 — Baseline Models
| Check | Result |
|---|---|
| DummyClassifier (majority-class, no class_weight) | PASS |
| Logistic Regression (class_weight="balanced") | PASS |
| Random Forest (class_weight="balanced") | PASS |
| confusion_matrices.png saved | OK |
| roc_curves.png saved | OK |
| rf_feature_importance.png saved | OK |
| lr_coefficients.png saved | OK |
| models/metadata_v1.json written | OK |
| reports/eval_v1.md written | OK |
| LR baseline gate (F1 + AUC > majority) | **PASS** |
| RF baseline gate (F1 + AUC > majority) | **PASS** |

---

## 4. Split Summary

| | Train | Test |
|---|---|---|
| Rows | 395 | 112 |
| Unique students | 64 | 16 |
| at_risk rate | 34.4% | 19.6% |
| Method | GroupShuffleSplit on academy_member_id | — |

No student appears in both train and test sets.

---

## 5. Model Metrics (MOCK DATA — NOT REAL FINDINGS)

### 5-Fold Cross-Validation (train set)

| Model | ROC AUC | F1 |
|---|---|---|
| majority_baseline | 0.5000 ± 0.0000 | 0.0000 |
| logistic_regression | 1.0000 ± 0.0000 | 0.9962 |
| random_forest | 1.0000 ± 0.0000 | 1.0000 |

### Held-out Test Set

| Model | ROC AUC | F1 | Precision | Recall |
|---|---|---|---|---|
| majority_baseline | 0.50 | 0.00 | 0.00 | 0.00 |
| logistic_regression | 1.00 | 1.00 | 1.00 | 1.00 |
| random_forest | 1.00 | 1.00 | 1.00 | 1.00 |

### Model Notes

- `majority_baseline`: DummyClassifier, `strategy="most_frequent"`, no `class_weight` — always predicts the most frequent label.
- `logistic_regression`: `class_weight="balanced"` applied to handle at_risk class imbalance.
- `random_forest`: `class_weight="balanced"` applied to handle at_risk class imbalance.

---

## 6. Interpretation

Perfect metrics (AUC = 1.0, F1 = 1.0) for LR and RF are **expected and correct behavior** for synthetic data.
The mock generator creates explicit risk patterns (score ranges, attempt counts, TTFC, hint_viewed) that are linearly separable by design.
These numbers confirm only that:

1. The pipeline executes without errors from end to end.
2. Leakage prevention rules are correctly enforced.
3. Student-level group splitting works correctly.
4. All artifact files are written to the correct paths.

They say nothing about model performance on real student data.

---

## 7. Artifacts Written by This Run

| File | Status | Notes |
|---|---|---|
| `notebooks/models/metadata_mock_v1.json` | Committed | Mock run metadata — not the real evaluation |
| `notebooks/reports/eval_mock_v1.md` | Committed | This file — mock run report |
| `notebooks/models/metadata_v1.json` | Committed | Skeleton from NB04 scaffold (will be overwritten by real run) |
| `notebooks/reports/eval_v1.md` | Committed | Skeleton from NB04 scaffold (will be overwritten by real run) |
| `notebooks/models/lr_v1.pkl` | **gitignored** | Not committed |
| `notebooks/models/rf_v1.pkl` | **gitignored** | Not committed |
| `notebooks/data/raw/*.csv` | **gitignored** | Not committed |
| `notebooks/data/processed/*.parquet` | **gitignored** | Not committed |
| `notebooks/reports/*.png` | **gitignored** | Not committed |

---

## 8. Real-Data Evaluation Status

**Status: PENDING**

Steps required before real evaluation:

1. Export session and attempt CSVs from `/admin/dataset` (production system).
2. Place files in `notebooks/data/raw/` with naming `session_YYYYMMDD_BATCHCODE.csv` / `attempt_YYYYMMDD_BATCHCODE.csv`.
3. Update `SNAPSHOT_DATE` and `BATCH_CODE` config variables in all 4 notebooks.
4. Run notebooks 01 → 02 → 03 → 04 in order.
5. Commit the real `notebooks/models/metadata_v1.json` and `notebooks/reports/eval_v1.md`.
6. Report real metrics in thesis.

---

*Generated: 2026-07-10 | Branch: feature/phase3-ai-risk-model | Phase 3 M3.1–M3.4*
